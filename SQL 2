/* In this SQL, I'm querying a database with multiple tables in MySQLite Studio  it to 



SELECT Name, LifeExpectancy AS "Life Expectancy"
FROM Country
ORDER BY Name;

Display the first 5 names in the Country table who reside in Europe.
SELECT Name, Continent, Region
FROM Country
WHERE Continent ='Europe'
ORDER BY Name
LIMIT 5;
